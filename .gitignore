/target
# Local artifacts
/cp2
/data

# Local-only benchmark against a personal remote (contains no credentials,
# but the host/user is personal) — keep it out of the repo.
/bench/compare_remote.sh

.zcode
# Release build artifacts (build-release.sh staging + tarball)
/cp2-0.1.1/
/cp2-*.tar.gz

# Local test artifacts (large copied trees)
/123/
